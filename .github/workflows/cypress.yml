name: Cypress Tests

on:
  push:
    branches: [ "v4", "v3"  ]
  pull_request:
    branches: [ "v4", "v3"  ]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress run
        uses: cypress-io/github-action@v4
        with:
          build: npm run build
